import { initThemeTweaks } from './theme-tweaks';
import { initCodeTabs, initClipboard } from './code-tabs';
import { initMiniCanvas } from './mini-canvas';

initThemeTweaks();
initCodeTabs();
initClipboard();
initMiniCanvas();
